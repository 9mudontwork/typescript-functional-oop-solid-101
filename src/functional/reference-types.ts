/**
 * pass by reference
 *
 * ส่งค่าที่อยู่ในตัวแปรเดิม มาใช้งาน
 *
 * เมื่แก้ไขข้อมูลบางอย่างใน ref มันอาจจะกระทบด้านนอก หรือ ที่อื่น ๆ
 */

type IReferenceTypes = {
  object: object
  array: Array<any>
}
